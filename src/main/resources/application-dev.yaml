spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/isp_db
    username: postgres
    password: postgres
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

jwt:
  secret: ${JWT_SECRET:a-very-long-random-secret-key-that-is-long-enough-change-this-in-production}